# StudentNest - Backend

Backend for StudentNest built with Node.js, Express, MongoDB.

Quick start:

1. Copy `.env.example` to `.env` and fill values.
2. Install deps: `npm install`.
3. Run locally: `npm run dev`.

Endpoints: See routes under `server/routes`.

Security and production notes included in `DEPLOYMENT.md`.
